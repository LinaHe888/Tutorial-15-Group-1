# Task 1 Screenshot Plan and Captions

## Recommended screenshots for Task 1

### Screenshot 1 — Terminal / decompilation process
**What to capture:**
- Terminal output showing `apktool` / `jadx` execution or analysis workspace setup.

**Current evidence file:**
- `submission/task-1-decompilation/my-task1-analysis/evidence/step-1-terminal-screenshot.jpg`

**Suggested caption:**
> Figure 1. Initial Task 1 setup and APK decompilation process using reverse engineering tools.

**Why it matters:**
- Shows that the APK was actually processed and that the workflow was executed.

---

### Screenshot 2 — AndroidManifest.xml (package name + launcher activity)
**What to capture:**
- `AndroidManifest.xml`
- Include line showing `package="com.example.mastg_test0016"`
- Include the `MainActivity` block with `MAIN` and `LAUNCHER`

**File:**
- `submission/task-1-decompilation/my-task1-analysis/apktool/AndroidManifest.xml`

**Suggested caption:**
> Figure 2. Decompiled Android manifest showing the package name and launcher activity (`MainActivity`).

**Why it matters:**
- This is the strongest evidence for package identification and app entry point recovery.

---

### Screenshot 3 — Login.java (class + login flow)
**What to capture:**
- Class declaration: `public class Login extends AppCompatActivity`
- Login button click logic
- `checkCredentials(...)`
- `createSession()` call on successful login

**File:**
- `submission/task-1-decompilation/my-task1-analysis/jadx/sources/com/example/mastg_test0016/Login.java`

**Suggested caption:**
> Figure 3. Decompiled `Login.java` showing the authentication flow, including credential validation and session creation.

**Why it matters:**
- Confirms that the authentication-related class was recovered and is readable after decompilation.

---

### Screenshot 4 — Decompiled output directories (optional)
**What to capture:**
- Finder/editor/terminal view showing both:
  - `apktool/`
  - `jadx/`

**Suggested caption:**
> Figure 4. Decompiled output structure generated by `apktool` and `jadx`.

**Why it matters:**
- Useful if you want one visual proof that both decompilation outputs were created.

---

## Best set if you only want 3 screenshots
If you want the minimum strong set, use:
1. Terminal / setup screenshot
2. Manifest screenshot
3. Login.java screenshot

---

## Practical advice
- Prioritize clarity over quantity.
- Make sure file name / class name is visible in the editor.
- Do not crop out the key evidence line.
- If possible, use one screenshot per claim.

---

## Suggested insertion order in Task 1 write-up
1. Figure 1 — decompilation workflow
2. Figure 2 — manifest evidence
3. Figure 3 — login class evidence
4. Figure 4 — optional supporting structure
